{
  "name": "@synergy/db",
  "version": "0.1.0",
  "private": true,
  "description": "Shared data store + types (JSON-backed today, Prisma in STEP 2).",
  "type": "module",
  "main": "./src/store.ts",
  "types": "./src/store.ts",
  "exports": {
    ".": "./src/store.ts",
    "./types": "./src/types.ts",
    "./seed": "./src/seed.ts"
  },
  "scripts": {
    "lint": "echo 'lint db (no-op)'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
