{
  "name": "@synergy/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared design system — Tailwind tokens, shadcn/ui primitives, brand components.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'lint scaffold'",
    "typecheck": "echo 'typecheck scaffold'",
    "clean": "rm -rf dist .turbo"
  }
}
