{
  "name": "@synergy/db",
  "version": "0.1.0",
  "private": true,
  "description": "Prisma schema + typed client for the Synergy Typing database (PostgreSQL / Neon).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'lint scaffold'",
    "typecheck": "echo 'typecheck scaffold'",
    "clean": "rm -rf dist .turbo"
  }
}
