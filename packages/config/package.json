{
  "name": "@synergy/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared tsconfig, eslint, tailwind preset, and brand tokens.",
  "files": ["tsconfig", "eslint", "tailwind", "tokens"],
  "scripts": {
    "lint": "echo 'lint scaffold'",
    "typecheck": "echo 'typecheck scaffold'"
  }
}
